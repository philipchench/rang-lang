f = func(x, y) {
    while (true) {
        if (!x || y) {
            meow();
        } else {
            break;
        }
        floor(x, y);
        x = x - 1;
        power(x, y);
    }
    return;
}